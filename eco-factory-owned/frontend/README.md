# Eco Factory Frontend

This image packages the currently verified Eco Factory static frontend so it can
be deployed from Verdixgreen-owned source instead of the previous third-party
GitLab image.

The bundled JavaScript file is the production hotfix version that removes Amata
from the member dashboard and keeps it under the main menu.
